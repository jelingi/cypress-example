"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExtractExecutionIssueTypeCommand = void 0;
const dedent_1 = require("../../../util/dedent");
const help_1 = require("../../../util/help");
const command_1 = require("../../command");
class ExtractExecutionIssueTypeCommand extends command_1.Command {
    allIssueDetails;
    constructor(parameters, logger, allIssueDetails) {
        super(parameters, logger);
        this.allIssueDetails = allIssueDetails;
    }
    async computeResult() {
        const allIssueDetails = await this.allIssueDetails.compute();
        const executionIssueDetails = allIssueDetails.filter((details) => details.name === this.parameters.testExecutionIssueType);
        if (executionIssueDetails.length === 0) {
            throw new Error((0, dedent_1.dedent)(`
                    Failed to retrieve Jira issue type information of test execution issue type called: ${this.parameters.testExecutionIssueType}

                    Make sure Xray's issue types have been added to project ${this.parameters.projectKey} or that you've configured any custom execution issue type accordingly

                      For example, the following plugin configuration will tell Xray to create Jira issues of type "My Custom Issue Type" to document test execution results:

                        {
                          jira: {
                            testExecutionIssueType: "My Custom Issue Type"
                          }
                        }

                    For more information, visit:
                    - ${help_1.HELP.plugin.configuration.jira.testExecutionIssueType}
                    - ${this.parameters.displayCloudHelp
                ? help_1.HELP.xray.issueTypeMapping.cloud
                : help_1.HELP.xray.issueTypeMapping.server}
                `));
        }
        else if (executionIssueDetails.length > 1) {
            throw new Error((0, dedent_1.dedent)(`
                    Failed to retrieve Jira issue type information of test execution issue type called: ${this.parameters.testExecutionIssueType}

                    There are multiple issue types with this name, make sure to only make a single one available in project ${this.parameters.projectKey}:

                      ${executionIssueDetails
                .map((details) => `${details.name}: ${JSON.stringify(details)}`)
                .join("\n")}

                    If none of them is the test execution issue type you're using in project ${this.parameters.projectKey}, please specify the correct text execution issue type in the plugin configuration:

                      {
                        jira: {
                          testExecutionIssueType: "My Custom Issue Type"
                        }
                      }

                    For more information, visit:
                    - ${help_1.HELP.plugin.configuration.jira.testExecutionIssueType}
                    - ${this.parameters.displayCloudHelp
                ? help_1.HELP.xray.issueTypeMapping.cloud
                : help_1.HELP.xray.issueTypeMapping.server}
                `));
        }
        return executionIssueDetails[0];
    }
}
exports.ExtractExecutionIssueTypeCommand = ExtractExecutionIssueTypeCommand;
