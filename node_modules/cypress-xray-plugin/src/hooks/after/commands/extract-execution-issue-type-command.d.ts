import { IssueTypeDetails } from "../../../types/jira/responses/issue-type-details";
import { InternalJiraOptions } from "../../../types/plugin";
import { Logger } from "../../../util/logging";
import { Command, Computable } from "../../command";
type Parameters = Pick<InternalJiraOptions, "projectKey" | "testExecutionIssueType"> & {
    displayCloudHelp: boolean;
};
export declare class ExtractExecutionIssueTypeCommand extends Command<IssueTypeDetails, Parameters> {
    private readonly allIssueDetails;
    constructor(parameters: Parameters, logger: Logger, allIssueDetails: Computable<IssueTypeDetails[]>);
    protected computeResult(): Promise<IssueTypeDetails>;
}
export {};
